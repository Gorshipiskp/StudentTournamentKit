# packages/api-types — Контракт API

Типы/клиент, генерируемые из OpenAPI FastAPI (`apps/api`). Используют overlay, dashboard, judge.

**Статус:** заготовка. Генерация появится, когда появятся стабильные эндпоинты сверх health.
