# apps/dashboard — Панель режиссёра и организатора

SvelteKit UI: турниры, сетка, матчи, управление OBS-сценами через Director Agent, статус эфира.

**Статус:** заготовка (UI вне scope TZ001).

Связь: REST/WS → `apps/api`; OBS — только через `apps/director-agent` (инвариант A8).
